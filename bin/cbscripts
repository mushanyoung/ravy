#!/usr/bin/env bash

_getpaste () { pbpaste; }

content=$(_getpaste)

pattern='^(open):\[([^]]*)\]$'

if [[ $content =~ $pattern ]]; then
  echo "match: '${BASH_REMATCH[0]}'"
  action="${BASH_REMATCH[1]} ${BASH_REMATCH[2]}"
  echo "$action"
  $action
fi
