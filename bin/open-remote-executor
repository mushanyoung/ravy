#!/usr/bin/env bash

_getpaste () { pbpaste; }

content=$(_getpaste)

pattern=$'^(open)\x0d(.*)$'

if [[ $content =~ $pattern ]]; then
  op=${BASH_REMATCH[1]}
  arg=$(head -1 <<< "${BASH_REMATCH[2]}")
  action="$op \$""'${arg//\'/\'}'"
  echo "$action"
  eval "$action"
fi
